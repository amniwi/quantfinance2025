These notebooks should be run in alphabetical order (0, 1, 2).

Notebook 0
Creates the training data for the LSTM models.

Notebook 1
Trains the LSTM models and determines which is best (based on mean squared error).

Notebook 2
Tests the value of the best LSTM model.

